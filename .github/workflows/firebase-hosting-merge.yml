# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install Dependencies
        run: npm ci

      - name: Build
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
          VITE_CLOUD_FUNCTIONS_API_KEY: ${{ secrets.CLOUD_FUNCTIONS_API_KEY }}
          VITE_CLOUD_FUNCTIONS_ENROLLCUSTOMER_URL: 'https://us-central1-trove-development-2a92d.cloudfunctions.net/enrollCustomer'
          VITE_CLOUD_FUNCTIONS_GETLOYALTYCARDINFO_URL: 'https://us-central1-trove-development-2a92d.cloudfunctions.net/getLoyaltyCardInfo'
          VITE_CLOUD_FUNCTIONS_EARNPOINTS_URL: 'https://us-central1-trove-development-2a92d.cloudfunctions.net/earnPoints'
          VITE_CLOUD_FUNCTIONS_CLAIMREWARD_URL: 'https://us-central1-trove-development-2a92d.cloudfunctions.net/claimReward'
        run: npm run build

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TROVE_DEVELOPMENT_2A92D }}
          channelId: live
          projectId: trove-development-2a92d
